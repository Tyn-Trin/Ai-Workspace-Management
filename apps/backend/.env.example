PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ai_mail_priority

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

SESSION_COOKIE_SECRET=change-me

AI_SERVICE_URL=http://localhost:8000
INTERNAL_SECRET=change-me
CRON_SECRET=change-me

# Pub/Sub push OIDC verify — เว้นว่างได้ตอน dev (ยังไม่มี push subscription จริง)
# ต้องตั้งทั้งคู่ก่อนขึ้น production ไม่งั้น /webhook/gmail ไม่มีการยืนยันตัวตนเลย
PUBSUB_AUDIENCE=
PUBSUB_SERVICE_ACCOUNT_EMAIL=
